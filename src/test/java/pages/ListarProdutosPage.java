package pages;

import org.kohsuke.rngom.parse.host.Base;
import org.openqa.selenium.WebDriver;

public class ListarProdutosPage extends BasePage {
    public ListarProdutosPage(WebDriver navegador) {
        super(navegador);
    }
}
